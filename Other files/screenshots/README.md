# Screenshots referenced from `Documentation.txt`

Drop screenshots here using the filename mentioned in the doc entry that
references them. The doc references the file by relative path so once you
save the image at the right name, links resolve.

Currently expected files:

- `arrival_drive_flat_durations.png` — referenced from entry [18]. Shows
  the analytics `Activities (raw)` sheet pre-fix with every `ArrivalDrive`
  duration flat at 240s / 4.00 min, motivating the per-track-distance fix.
